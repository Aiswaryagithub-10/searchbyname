import React from 'react';
import ReactDOM from 'react-dom';
import './App.css';
import App from './App';

// If reportWebVitals is not needed, this can be removed
// import reportWebVitals from './reportWebVitals';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// If you want to log web vitals, you can uncomment this and include reportWebVitals.js
// reportWebVitals();
